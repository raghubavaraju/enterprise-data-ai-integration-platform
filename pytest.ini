[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = --strict-markers -ra
markers =
    integration: requires the full stack to be running (make run / make up)
filterwarnings =
    ignore::DeprecationWarning
